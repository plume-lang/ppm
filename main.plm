require "library/parser"

source = "hello world"

parser = string("hello") >> string(" world")
res = parser.eval_parser(source)

switch res 
  case Ok(x) => println(x[1..])
  case Error(err) => println("Error: " + err)